# Anthropic Claude API Key
# Get your API key from: https://console.anthropic.com/
#
# SECURITY NOTE:
# - Copy this file to .env.local and add your actual API key
# - NEVER commit .env.local to version control (already in .gitignore)
# - The API key is used server-side only in Next.js API routes
# - For production, use environment variables in your hosting platform (Vercel, etc.)
#
ANTHROPIC_API_KEY=your_api_key_here

# Apple Wallet Certificates
# Certificate files are stored in features/wallet/certs/
# See features/wallet/certs/README.md for setup instructions
# These files are NOT committed to version control for security
